{
  "name": "keele-scm-module-evaluation",
  "version": "0.0.1",
  "main": "src/index.js",
  "author": "Chris Briggs <c.briggs@keele.ac.uk>",
  "license": "MIT",
  "scripts": {
    "import_data": "mkdir -p dist && cp -R ../data/generated/ dist/data/",
    "build": "yarn import_data && parcel build src/index.html",
    "develop": "yarn import_data && parcel --hmr-port 8080 src/index.html",
    "serve": "pm2 serve dist/ 80 --no-daemon"
  },
  "devDependencies": {
    "parcel-bundler": "^1.8.1",
    "pm2": "^2.10.4"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.2.1",
    "vega": "^3.3.1",
    "vega-embed": "^3.9.0",
    "vega-lite": "^2.4.1"
  }
}
